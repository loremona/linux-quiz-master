/* MODULO 4 — Banca domande extra (drill + esami). Dischi, FHS, permessi (104). */
'use strict';
const MODULE04_QUIZ = [
  { type:'quiz', q:'Quale notazione ottale corrisponde ai permessi rw-r--r--?',
    opts:['644','755','600','664'], a:0,
    explain:`rw- = 6, r-- = 4, r-- = 4 → <strong>644</strong>. È il default tipico dei file.` },
  { type:'quiz', q:'Cosa significa chmod 755 su un file?',
    opts:['rwx per owner, r-x per gruppo e altri','rw- per tutti','rwx per tutti','r-- solo per owner'], a:0,
    explain:`7 = rwx (owner), 5 = r-x (gruppo), 5 = r-x (altri). Tipico di programmi/script eseguibili.` },
  { type:'input', q:'Quale comando crea un filesystem ext4 sulla partizione /dev/sdb1?',
    accept:['mkfs.ext4 /dev/sdb1','mkfs -t ext4 /dev/sdb1'], placeholder:'comando...',
    explain:`<code>mkfs.ext4 /dev/sdb1</code> (o <code>mkfs -t ext4</code>).` },
  { type:'quiz', q:'Quale comando monta tutte le voci elencate in /etc/fstab?',
    opts:['mount -a','mount --fstab','mountall','mount -F'], a:0,
    explain:`<code>mount -a</code> monta tutto ciò che è in fstab (e non è già montato).` },
  { type:'quiz', q:'Quale comando controlla e ripara un filesystem (da smontato)?',
    opts:['fsck','mkfs','df','tune2fs'], a:0,
    explain:`<code>fsck /dev/sdaN</code>. Mai su filesystem montato in scrittura! L\'ultimo campo di fstab decide l\'ordine al boot.` },
  { type:'quiz', q:'Cosa fa il bit SUID su un file eseguibile?',
    opts:['Lo esegue col privilegio del proprietario del file','Lo esegue col privilegio di chi lo lancia','Ne impedisce la cancellazione','Lo rende di sola lettura'], a:0,
    explain:`SUID = esegue con l\'UID del proprietario (es. <code>passwd</code> gira come root). Valore ottale <strong>4000</strong>.` },
  { type:'quiz', q:'Lo sticky bit su /tmp serve a...',
    opts:['Permettere a ognuno di cancellare SOLO i propri file','Far cancellare tutto a tutti','Impedire ogni scrittura','Eseguire come root'], a:0,
    explain:`Sticky bit (ottale <strong>1000</strong>): in una dir condivisa, solo il proprietario di un file può rimuoverlo.` },
  { type:'quiz', q:'Cosa fa il bit SGID su una directory?',
    opts:['I nuovi file ereditano il gruppo della directory','Rende i file eseguibili','Solo root può scrivere','Cancella i file vecchi'], a:0,
    explain:`SGID su dir (ottale 2000): i file creati dentro ereditano il gruppo della directory, utile per cartelle condivise.` },
  { type:'quiz', q:'Con umask 022, quali permessi ha un nuovo FILE?',
    opts:['644','755','666','600'], a:0,
    explain:`I file partono da 666; 666 − 022 = <strong>644</strong>. (Le directory partono da 777 → 755.)` },
  { type:'quiz', q:'Quale comando mostra lo spazio LIBERO/usato dei filesystem montati?',
    opts:['df -h','du -sh','lsblk','fdisk -l'], a:0,
    explain:`<code>df -h</code> (disk free) per filesystem. <code>du</code> misura lo spazio usato da file/cartelle.` },
  { type:'quiz', q:'Quale comando mostra lo spazio occupato da una directory (ricorsivo, totale)?',
    opts:['du -sh cartella','df -h','ls -l','free -h'], a:0,
    explain:`<code>du -sh cartella</code>: <code>-s</code> totale, <code>-h</code> leggibile.` },
  { type:'quiz', q:'Differenza tra hard link e soft (symbolic) link?',
    opts:['L\'hard link punta allo stesso inode; il soft link è un puntatore a un percorso','Sono identici','Il soft link punta all\'inode','L\'hard link funziona tra filesystem diversi'], a:0,
    explain:`Hard link = stesso inode (stesso dato). Soft link = file che contiene un percorso; può puntare a un altro filesystem ma si rompe se l\'originale sparisce.` },
  { type:'quiz', q:'È possibile creare un hard link tra due filesystem diversi?',
    opts:['No, mai','Sì, sempre','Solo per root','Solo su ext4'], a:0,
    explain:`Gli hard link condividono l\'inode, che è locale al filesystem: per attraversare filesystem serve un soft link.` },
  { type:'quiz', q:'Quale comando cambia proprietario E gruppo di un file in un\'unica operazione?',
    opts:['chown utente:gruppo file','chgrp utente file','chmod owner file','chown -g file'], a:0,
    explain:`<code>chown utente:gruppo file</code>. <code>chgrp</code> cambia solo il gruppo.` },
  { type:'quiz', q:'Quale comando aggiorna il database usato da "locate"?',
    opts:['updatedb','locate -u','mlocate','find --update'], a:0,
    explain:`<code>updatedb</code> (di solito eseguito da cron). <code>locate</code> è velocissimo ma legge un database, non il disco in tempo reale.` },
  { type:'quiz', q:'Quale directory dell\'FHS contiene i file di configurazione di sistema?',
    opts:['/etc','/var','/usr','/opt'], a:0,
    explain:`<code>/etc</code> = configurazioni. <code>/var</code> = dati variabili (log, mail), <code>/usr</code> = programmi/librerie, <code>/opt</code> = software opzionale di terze parti.` },
];
